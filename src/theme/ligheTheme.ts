import { colors } from "@/theme/colors";

export const lightTheme = {
  mode: "light",
  colors,
  components: {
    headerLogoutButton: {
      bg: colors.red,
      bgHover: "#000000",
      textColor: colors.white,
    },
  },
};
